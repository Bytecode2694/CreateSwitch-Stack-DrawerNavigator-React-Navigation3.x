import { Directions } from "react-native-gesture-handler";

export default {
    viewlayout: {
        flex:1,
        backgroundColor: "#fff",
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    }
}